# Labs

Hands-on labs that validate Stave controls against real AWS misconfigurations. Each lab creates a deliberately vulnerable environment, runs `stave apply`, and verifies the findings.

Pick a domain and work through the scenario.
